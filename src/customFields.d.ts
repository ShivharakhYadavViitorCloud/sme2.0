declare module '@docusaurus/module-type-aliases' {
  interface CustomFields {
    /** Base URL of the SME API server (see `server/`), e.g. http://localhost:8787 */
    publicApiBaseUrl?: string;
  }
}
